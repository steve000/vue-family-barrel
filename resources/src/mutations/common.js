import * as C from '../constants/api'
const state = {

}

const mutations = {
    // 这里的payload相当于回调
    [C.QUERY_PROVINCE_LIST](state, {
        payload
    }) {

    },
    [C.QUERY_CITY_LIST](state, {
        payload
    }) {

    },
    [C.QUERY_AREA_LIST](state, {
        payload
    }) {

    }
}

export default {
    state,
    mutations
}
