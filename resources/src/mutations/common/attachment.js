import * as C from '../../constants/api'

const state = {}

const mutations = {
    // 查询业务关联附件列表
    [C.QUERY_MODULE_ATTRLIST](state, {
        payload
    }) {}
}
export default {
    state,
    mutations
}
